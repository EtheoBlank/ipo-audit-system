# Bug Scan Report — 2026-06-18

4 个 Explore agent 并行扫了 4 个层, 找到 ~60+ 个 bug. 优先级分类如下:

## P0 (必须修) — 22 个

### A. 多租户隔离漏洞 (10 个, 大量端点)
- `app/api/reports.py` anomalies 端点无 `ensure_project_in_firm`
- `app/api/inventory.py` **30+** 端点用 `get_project_or_404` 替代 `ensure_project_in_firm`
- `app/api/confirmations.py` **20+** 端点用 `_get_*_or_404` 替代
- `app/api/account_audit.py` L256/294 `ensure_project_in_firm` 在 service 写库后校验
- `app/api/comprehensive.py` firm_id 客户端 Form/Query 接受, 未绑定 `current_user.firm_id`
- `app/api/report_templates.py` 同上
- `app/api/sentiment.py` **20+** 端点 (subjects/sources/events/briefings/reports)
- `app/api/related_parties.py` 5 个 list 端点
- `app/api/ipo_specials.py` 6 个 list 端点
- `app/api/sentiment.py` L1187 scheduler start/stop 不限 admin, 任何登录用户可停服
- `app/api/auth.py` L747 list_approvals 不按 firm 过滤

### B. 前端 (4 个)
- `frontend/app.py` page session_state hack 与 sidebar radio 抢状态
- `frontend/pages_confirmations.py` 5 处 `user_name` 默认值 ('审计师') 多人协同不可分
- `frontend/pages_comprehensive.py` `/tmp/comp_template_anon.xlsx` 多用户覆盖
- `frontend/pages_sentiment.py` L165 `pd.Timestamp` 传给 `date_input` 类型错

### C. 配置/测试 (2 个)
- `pyproject.toml` 缺 `[tool.pytest.ini_options] asyncio_mode = "auto"`, async test 60+ 静默跳过
- `app/api/auth.py` L789 审批 expected_version Optional, 客户端不传无 409 兜底

### D. 业务算法 P0 (5 个)
- `app/services/knowledge_base/embedder.py` TfidfEmbedder.fit 每次重算词表, 跨书 cosine=0
- `app/services/inventory/aging_engine.py` FIFO 期初 365 天硬编码, 库龄严重高估
- `app/services/account_audit/__init__.py` 恒等式公式按余额方向反, 贷方科目假阳性
- `app/services/sales_ledger/analyzer.py` L329 return_amount 扣减逻辑错位
- `app/services/trial_balance.py` L186 bank_statements NaN 序列化 500

### E. 安全 (1 个)
- `app/services/excel_parser.py` L73/L107/L147 文件名未 sanitize, 路径穿越

## P1 (重要) — 25+ 个

集中在: async 端点同步阻塞 (download 4 处), 索引缺失 (AuditLog/SentimentSource/ApprovalStep),
测试覆盖空白 (audit_cycles 22 ORM 0 测试), cascade 不对称, 性能 N+1, encoding (xlrd 引擎),
cascade (Blocker/Tenant), 业务口径 (AI 收入前缀不全, SAP 借贷方向反, 供应商 LIKE 过窄)

## P2 (代码质量) — 15+ 个

Pydantic 校验, fixture 风格, label_map fallback, Python 列表 (regulations favorites 不分页),
read-side API (kb_stats 4 次 SELECT), 评级累加重叠 (quality_assessor)

## 修复策略

- 阶段 1: A (多租户) + B (前端) + C (配置) — 25 个 P0 一次性修
- 阶段 2: D (业务) + E (安全) — 选 3-5 个最高影响
- 阶段 3: P1 性能/索引批改
- 阶段 4: P2 留作下轮
